# Recommender System — Instacart Market Basket Analysis
# Python 3.9+
#
# Dependencies: numpy, scipy, pandas, networkx, tqdm, numba.

# Data loading & processing
pandas>=1.3.0
numpy>=1.21.0

# Sparse matrix operations (SPMI, co-occurrence)
scipy>=1.7.0

# Knowledge Graph (graph building)
networkx>=2.6.0

# Progress bars (cho chunk processing)
tqdm>=4.62.0

# RAM monitoring
psutil>=5.8.0

# JIT compilation for hot loops (co-occurrence, skip-gram)
numba>=0.56.0