# Recommender System — Instacart Market Basket Analysis
# Python 3.9+
#
# Dependencies: numpy, scipy, pandas, scikit-learn, gensim, tqdm.

# Data loading & processing
pandas>=1.3.0
numpy>=1.21.0

# Sparse matrix operations (co-occurrence)
scipy>=1.7.0

# TF-IDF, cosine similarity, OneHotEncoder
scikit-learn>=1.0.0

# Word2Vec (Item2Vec, Node2Vec)
gensim>=4.2.0

# Parquet read/write (bat buoc cho pandas read_parquet/to_parquet)
pyarrow>=14.0.0

# Progress bars (cho chunk processing)
tqdm>=4.62.0
