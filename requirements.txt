# Recommender System — Instacart Market Basket Analysis
# Python 3.9+

# Data loading & processing
pandas>=1.3.0
numpy>=1.21.0

# Sparse matrix operations (SPMI, co-occurrence)
scipy>=1.7.0

# Content-Based (TF-IDF, cosine similarity)
scikit-learn>=1.0.0

# Knowledge Graph (graph building)
networkx>=2.6.0

# Knowledge Graph (node2vec embeddings)
node2vec>=0.4.0
gensim>=4.1.0

# Progress bars (optional, cho chunk processing)
tqdm>=4.62.0