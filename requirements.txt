# Recommender System — Instacart Market Basket Analysis
# Python 3.9+
#
# Nguyên tắc: KHÔNG dùng thư viện ML có sẵn (sklearn, node2vec, gensim...)
# Tất cả thuật toán (TF-IDF, node2vec, skip-gram, cosine similarity)
# đều được tự code từ đầu, chỉ dùng numpy + scipy + networkx.

# Data loading & processing
pandas>=1.3.0
numpy>=1.21.0

# Sparse matrix operations (SPMI, co-occurrence)
scipy>=1.7.0

# Knowledge Graph (graph building)
networkx>=2.6.0

# Progress bars (cho chunk processing)
tqdm>=4.62.0
